export interface filterData {
	discount_price: number[]
	sex: string[]
	category: string[]
	color: string[]
	size: string[]
	brand: string[]
}

export interface filterParamsType {
	price: number[]
	pol: string[]
	kategoriya: string[]
	tsvet: string[]
	razmer: string[]
	brand: string[]
}
